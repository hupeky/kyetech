import React from 'react'
import HomeBanner from '../../Containers/HomeBanner/HomeBanner'

const home = (props) => {
    console.log (props)
    return (
        <HomeBanner />
    )
}

export default home
